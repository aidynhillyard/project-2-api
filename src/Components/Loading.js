const Loading = () => {
  return (
    <div className="loading">
      <p>Loading up to the first 45 results...</p>
      <p>For more relevant results, use specific keywords.</p>
    </div>
  );
};

export default Loading;
